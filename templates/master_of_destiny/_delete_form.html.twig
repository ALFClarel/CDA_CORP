<form method="post" action="{{ path('admin_app_mod_delete', {'id': master_of_destiny.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ master_of_destiny.id) }}">
    <button class="btn">Delete</button>
</form>
