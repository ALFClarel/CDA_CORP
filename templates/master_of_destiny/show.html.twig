{% extends 'base.html.twig' %}

{% block title %}MasterOfDestiny{% endblock %}

{% block body %}
    <h1>MasterOfDestiny</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ master_of_destiny.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ master_of_destiny.name }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ master_of_destiny.pseudo }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ master_of_destiny.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_master_of_destiny_index') }}">back to list</a>

    <a href="{{ path('app_master_of_destiny_edit', {'id': master_of_destiny.id}) }}">edit</a>

    {{ include('master_of_destiny/_delete_form.html.twig') }}
{% endblock %}
